node_modules/
dist/
.build/
.mastra/
.env
.env.*
!.env.example
*.log
.DS_Store
.turbo/
bun.lockb
.cache/

mastra.db
mastra.db-shm
mastra.db-wal

# Agent state: the LibSQL database holds conversation memory and the reminder
# queue. It is per-deployment and must never reach a public repo.
*.db
*.db-shm
*.db-wal

# Machine-local Docker override (declares itself as not-committed)
docker/docker-compose.override.yml
